namespace Xenopairings.Services.Reminders;

public interface IReminderService
{
    /// <summary>
    /// Placeholder — no reminder functionality yet.
    /// Returns a task that completes immediately.
    /// </summary>
    Task NoOpAsync();
}
